
  # Chrome Extension UI Design (Copy)

  This is a code bundle for Chrome Extension UI Design (Copy). The original project is available at https://www.figma.com/design/bLWhP3WzjFsV0eZImeVlTe/Chrome-Extension-UI-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  